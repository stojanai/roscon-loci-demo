# Vendored / derived — rebuildable, huge, or another repo's clone (see README setup)
ardupilot/
toolchain/
venv/
.loci-build/
*.tar.xz

# SITL telemetry logs
mav.tlog
mav.tlog.raw
*.tlog
*.tlog.raw

# OS cruft
.DS_Store

report-*-darwin.md
*.paths.json

# LOCI demo: SITL mission-upload helper log
mission/last_upload.log

# Conference outreach / sales targeting — internal, not published
roscon-2026-*.csv
