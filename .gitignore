# Vendored / derived — rebuildable, huge, or another repo's clone (see README setup)
ardupilot/
toolchain/
venv/
.loci-build/
*.tar.xz

# SITL telemetry logs
mav.tlog
mav.tlog.raw
*.tlog
*.tlog.raw

# LOCI bug-report / repro artifacts (transient)
loci-repro-*/
loci-repro-*.zip
report-*-darwin.md

# OS cruft
.DS_Store
